#!/bin/bash
# Lists WareWare backups

source /usr/local/bin/vars-include
echo
echo "Available backups:"

found=0
for dir in "$br"/*/; do
  [ -d "$dir" ] || continue
  basename "$dir"
  found=1
done

if [ "$found" -eq 0 ]; then
  echo "No backups found"
fi